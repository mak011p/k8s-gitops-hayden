---
apiVersion: v1
kind: Service
metadata:
  name: postgres-mcp
  namespace: ai-system
  labels:
    app.kubernetes.io/name: postgres-mcp
spec:
  selector:
    app.kubernetes.io/name: postgres-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
