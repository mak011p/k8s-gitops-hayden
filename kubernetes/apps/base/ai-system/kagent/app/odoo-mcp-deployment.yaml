---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: odoo-mcp
  namespace: ai-system
  labels:
    app.kubernetes.io/name: odoo-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: odoo-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: odoo-mcp
    spec:
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: odoo-mcp
          image: ghcr.io/astral-sh/uv:python3.12-bookworm-slim
          command:
            - /bin/sh
            - -c
            - |
              uvx --from mcp-server-odoo --with 'mcp[cli]>=1.8,<1.26' python -c "
              from mcp.server.transport_security import TransportSecuritySettings
              import mcp.server.fastmcp.server as fastmcp_mod
              _orig_init = fastmcp_mod.FastMCP.__init__
              def _patched_init(self, *args, **kwargs):
                  kwargs['transport_security'] = TransportSecuritySettings(enable_dns_rebinding_protection=False)
                  return _orig_init(self, *args, **kwargs)
              fastmcp_mod.FastMCP.__init__ = _patched_init
              from mcp_server_odoo.server import OdooMCPServer
              server = OdooMCPServer()
              server.app.settings.host = '0.0.0.0'
              server.app.settings.port = 8000
              server.app.run(transport='sse')
              "
          env:
            - name: UV_CACHE_DIR
              value: /tmp/uv-cache
            - name: UV_TOOL_DIR
              value: /tmp/uv-tools
            - name: HOME
              value: /tmp
            - name: ODOO_URL
              value: http://odoo.business-system.svc.cluster.local:8071
            - name: ODOO_DB
              value: prod
            - name: ODOO_USER
              valueFrom:
                secretKeyRef:
                  name: odoo-mcp-credentials
                  key: username
            - name: ODOO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: odoo-mcp-credentials
                  key: password
            - name: ODOO_YOLO
              value: "read"
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 18
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 512Mi
