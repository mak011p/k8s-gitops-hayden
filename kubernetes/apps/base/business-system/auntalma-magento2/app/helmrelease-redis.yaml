---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2beta2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: auntalma-redis
  namespace: business-system
spec:
  interval: 15m
  chartRef:
    kind: OCIRepository
    name: bitnami-redis-auntalma
    namespace: business-system
  install:
    timeout: 10m
    replace: true
    crds: CreateReplace
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      remediateLastFailure: true
      retries: 3
      strategy: rollback
    cleanupOnFail: true
    crds: CreateReplace
  test:
    enable: false
  rollback:
    recreate: true
    force: true
    cleanupOnFail: true
  uninstall:
    keepHistory: false
  driftDetection:
    mode: enabled
  maxHistory: 3
  values:
    # Redis 7.2 for Magento
    image:
      registry: docker.io
      repository: bitnami/redis
      tag: 7.2.6

    # Standalone mode (no replication for dev)
    architecture: standalone

    # Disable auth for internal cluster use
    auth:
      enabled: false

    # Master configuration
    master:
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          memory: 1Gi
      persistence:
        enabled: true
        storageClass: ceph-block
        size: 5Gi

    # Service
    service:
      type: ClusterIP
      ports:
        redis: 6379

    # Metrics
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
        namespace: business-system
        interval: 30s
