---
apiVersion: v1
kind: Service
metadata:
  name: odoo-mcp
  namespace: ai-system
  labels:
    app.kubernetes.io/name: odoo-mcp
spec:
  selector:
    app.kubernetes.io/name: odoo-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
