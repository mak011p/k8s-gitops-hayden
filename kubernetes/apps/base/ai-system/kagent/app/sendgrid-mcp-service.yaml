---
apiVersion: v1
kind: Service
metadata:
  name: sendgrid-mcp
  namespace: ai-system
  labels:
    app.kubernetes.io/name: sendgrid-mcp
spec:
  selector:
    app.kubernetes.io/name: sendgrid-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
